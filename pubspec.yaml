name: schemamodeschema
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.9.2

dependencies:
  build: ^2.4.1
  collection: ^1.19.0
  meta: ^1.11.0
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  build_runner: ^2.4.11
  test: ^1.25.6
